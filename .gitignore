.DS_Store
.idea/
.direnv/
result
result-*

# Generated by the git-hooks.nix shellHook on `nix develop` / direnv entry.
.pre-commit-config.yaml

# The consumer template ships no lock; Nix may regenerate one here when the
# template flake is evaluated, but consumers generate (and track) their own.
/templates/consumer/flake.lock
