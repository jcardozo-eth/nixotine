# Fork-local module (untracked; appended to extraModules).
# Your own casks/packages; list options merge with the base.
{ pkgs, ... }:
{
  # homebrew.casks = [ "your-app" ];
  # home-manager.users.you.home.packages = [ pkgs.your-tool ];
}
