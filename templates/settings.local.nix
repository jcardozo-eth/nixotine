# Fork-local settings (untracked; merged over settings.nix).
# Set only what differs from the defaults.
{
  username = "you";
  hostname = "your-host";
  git = {
    userName = "You";
    userEmail = "you@example.com";
  };
}
